#include <stdio.h>

int max(int a, int b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
    
int main() {
    int x = 5, y = 10;
    
    printf("Максимальное из %d и %d равно %d\n", x, y, max(x, y));
    
    return 0;
}
